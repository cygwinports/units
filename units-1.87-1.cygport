DESCRIPTION="Console unit convertor"
HOMEPAGE="http://www.gnu.org/software/units/"
SRC_URI="mirror://gnu/${PN}/${P}.tar.gz"
PATCH_URI="1.87-exeext.patch"

# the trailing slash is required
CYGCONF_ARGS="--datadir=/usr/share/${PN}/"

src_install() {
	cd ${B}
	cyginstall -j1
}
